/*
 * =============================================================================
 * Remove default wagons - they are not required anymore
 * =============================================================================
 */

if (!param_keep_original) {
	/* Steam / Diesel / Electric wagons */
	disable_item(FEAT_TRAINS, 29, 53);
	/* Monorail wagons */
	disable_item(FEAT_TRAINS, 59, 83);
	/* Maglev wagons */
	disable_item(FEAT_TRAINS, 91, 115);
}

